import { DEFAULT_DEFINITION } from '@splunk/dashboard-definition';
import type { DashboardJSON } from '@splunk/dashboard-types';

const dashboardDefinition: DashboardJSON = {
    ...DEFAULT_DEFINITION,
};

export default dashboardDefinition;
